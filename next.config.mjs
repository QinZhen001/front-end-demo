/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: "/front-end-demo/",
  basePath: "/front-end-demo",
  reactStrictMode: false,
  output: "export",
  // output: "standalone",
}

export default nextConfig
